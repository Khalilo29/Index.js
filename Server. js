const express = require('express');
const app = express();
app.get('/', (req, res) => {
    res.send('<h1 style="text-align:center;margin-top:50px;">🚀 منصة خليل تعمل بنجاح!</h1>');
});
app.listen(process.env.PORT || 3000);
